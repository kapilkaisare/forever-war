const DEFAULT_WIDTH = 1024
const DEFAULT_HEIGHT = 768

const PI = 3.14

const ANCHORX = 0.5
const ANCHORY = 0.5
const SCALEX = 0.5
const SCALEY = 0.5
const MARGIN = 64


const CONSTANTS = {
    DEFAULT_WIDTH,
    DEFAULT_HEIGHT,
    MATH: {
        PI
    },
    DEFAULT_SHIP_ATTR: {
        ANCHORX,
        ANCHORY,
        SCALEX,
        SCALEY
    },
    DEFAULT_FLEET_ATTR: {
        MARGIN
    }
}

export { CONSTANTS }

